.info {
  padding: 12px 15px;
  color: var(--primary-text-color);
  width: 100%;
  border-radius: 4px;
  line-height: 24px;

  &-warning {
    background: var(--system-yellow-level-1);
  }

  svg {
    float: left;
    margin-right: 2px;
  }
}
